# Every key a module ships must start with mod-<id>-. The master checks this on
# install, which is what keeps your keys from colliding with the panel's or
# another module's.
mod-{{id}}-title = Template
mod-{{id}}-greet = Greet new players
mod-{{id}}-greet-hint = Writes a line to the log when somebody joins for the first time.
mod-{{id}}-joins = Joins
